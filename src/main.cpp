#include <iostream>
#include "PostProcessing.h"

int main()
{
    // Uncomment the next line if you need to process the raw files into CSV files:
    // processAllFiles();

    // Count unique access points for all processed CSV files.
    countUniqueAPsForAllFiles();

    return 0;
}
